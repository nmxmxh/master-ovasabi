// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.5
//   protoc               v6.32.1
// source: common/v1/compute.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Struct } from "../../google/protobuf/struct";
import { Timestamp } from "../../google/protobuf/timestamp";
import { Metadata } from "./metadata";

export const protobufPackage = "common";

/**
 * Universal compute schema for device-agnostic distributed execution
 * Fits Nexus event bus using common.Payload and common.Metadata
 */

/** DataRef represents an input/output datum. */
export interface DataRef {
  name: string;
  /** e.g. application/json, application/octet-stream */
  contentType: string;
  inlineBytes?: Uint8Array | undefined;
  inlineJson?:
    | { [key: string]: any }
    | undefined;
  /** s3://, https://, ipfs://, file://wasi/... */
  uri?:
    | string
    | undefined;
  /** checksum, shape, dtype, layout, etc. */
  annotations: { [key: string]: string };
}

export interface DataRef_AnnotationsEntry {
  key: string;
  value: string;
}

/** TensorSpec describes tensor metadata for validation and placement. */
export interface TensorSpec {
  /** f16,f32,i8,u8,bf16,... */
  dtype: string;
  /** e.g. [1,3,224,224] */
  shapes: string[];
  /** NCHW, NHWC, CHW, HWC */
  layout: string;
  /** cpu or gpu */
  deviceHint: string;
  quant?: TensorSpec_Quantization | undefined;
}

/** Optional quantization parameters */
export interface TensorSpec_Quantization {
  scale: number;
  zeroPoint: string;
  /** per_tensor, per_channel */
  scheme: string;
}

/** BufferSpec describes low-level buffer constraints. */
export interface BufferSpec {
  byteLength: string;
  stride: number;
  offset: number;
  alignment: number;
}

/** GPUDescriptor captures GPU/backend information across environments. */
export interface GPUDescriptor {
  /** webgpu|cuda|metal|vulkan|directml|rocm */
  backend: string;
  /** nvidia|amd|intel|apple|qualcomm|arm|unknown */
  vendor: string;
  adapterName: string;
  adapterUuid: string;
  /** shader_f16, subgroup, timestamp_query, storage_64... */
  features: string[];
  /** maxThreadsPerGroup, maxBufferSize, maxBindGroups... */
  limits: { [key: string]: string };
}

export interface GPUDescriptor_LimitsEntry {
  key: string;
  value: string;
}

/** Capability advertises a worker's abilities and resources. */
export interface Capability {
  wasm: boolean;
  threads: boolean;
  simd: boolean;
  webgpu: boolean;
  cpuCores: number;
  memoryMb: number;
  /** optional */
  gpu?:
    | GPUDescriptor
    | undefined;
  /** camera, low-power, 5g, wifi */
  labels: string[];
  /** os, arch, browser, driver_version... */
  attributes: { [key: string]: string };
  /** device traits, trust, energy, network, location buckets */
  metadata?: Metadata | undefined;
}

export interface Capability_AttributesEntry {
  key: string;
  value: string;
}

/** Requirements expresses hard and soft constraints. */
export interface Requirements {
  /** must-have */
  min?:
    | Capability
    | undefined;
  /** nice-to-have */
  preferred?:
    | Capability
    | undefined;
  /** priority, deadline_ms, retries, preemptible, budget_ms */
  qos: { [key: string]: string };
  /** sandbox|trusted|exclusive */
  isolation: string;
  /** region, az, affinity labels */
  locality: { [key: string]: string };
  /** policy, pricing/SLA context, provenance */
  metadata?:
    | Metadata
    | undefined;
  /** New field for parallelism configuration */
  parallelism?: ParallelismConfig | undefined;
}

export interface Requirements_QosEntry {
  key: string;
  value: string;
}

export interface Requirements_LocalityEntry {
  key: string;
  value: string;
}

/** ParallelismConfig defines how a task should be parallelized. */
export interface ParallelismConfig {
  /** e.g., "map", "scatter", "none" */
  strategy: string;
  /** Max number of chunks to create. 0 for auto/unlimited. */
  maxChunks: number;
}

/** ModuleSpec defines how to locate and invoke the module. */
export interface ModuleSpec {
  /** wasm|builtin|container */
  kind: string;
  /** module location (for wasm/container) */
  uri: string;
  /** sha256 or similar for integrity */
  hash: string;
  /** function name e.g. runGPUCompute */
  entry: string;
  expectedInputs: TensorSpec[];
  expectedOutputs: TensorSpec[];
  /** hyperparameters */
  params: { [key: string]: string };
  /** wasi_fs, net, gpu_access, memory_limit_mb */
  permissions: { [key: string]: string };
  /** module provenance, signature, policy */
  metadata?: Metadata | undefined;
}

export interface ModuleSpec_ParamsEntry {
  key: string;
  value: string;
}

export interface ModuleSpec_PermissionsEntry {
  key: string;
  value: string;
}

/** ComputeEnvelope is the task definition carried over the event bus. */
export interface ComputeEnvelope {
  taskId: string;
  /** logical operation id */
  op: string;
  /** semver of op */
  version: string;
  module?: ModuleSpec | undefined;
  requirements?: Requirements | undefined;
  inputs: DataRef[];
  params: { [key: string]: string };
  /** event type/topic for results */
  returnChannel: string;
  /** task_token, signature, requester_id */
  security: { [key: string]: string };
  /** carry timing hints */
  notBefore?: Date | undefined;
  deadline?:
    | Date
    | undefined;
  /** task-level provenance (requester, purpose, tags) */
  metadata?: Metadata | undefined;
}

export interface ComputeEnvelope_ParamsEntry {
  key: string;
  value: string;
}

export interface ComputeEnvelope_SecurityEntry {
  key: string;
  value: string;
}

/** Claim emitted by a worker or used in assignment acknowledgement. */
export interface ComputeClaim {
  taskId: string;
  workerId: string;
  capabilities?:
    | Capability
    | undefined;
  /** bid score, rationale, current load */
  metadata?: Metadata | undefined;
}

/** Assignment confirms scheduler routing. */
export interface ComputeAssignment {
  taskId: string;
  workerId: string;
  metadata: { [key: string]: string };
  /** routing, pricing, SLA snapshot */
  meta?: Metadata | undefined;
}

export interface ComputeAssignment_MetadataEntry {
  key: string;
  value: string;
}

/** Progress updates with optional metrics. */
export interface ComputeProgress {
  taskId: string;
  /** 0..100 */
  pct: number;
  note: string;
  /** throughput, tokens_s, tf_ops, mem_used_mb */
  metrics: { [key: string]: string };
  /** device health, counters, provenance */
  metadata?: Metadata | undefined;
}

export interface ComputeProgress_MetricsEntry {
  key: string;
  value: string;
}

/** Result carries outputs and a brief summary for indexing. */
export interface ComputeResult {
  taskId: string;
  outputs: DataRef[];
  /** quick, queryable fields */
  summary: { [key: string]: string };
  /** proofs, quality, audit, lineage */
  metadata?: Metadata | undefined;
}

export interface ComputeResult_SummaryEntry {
  key: string;
  value: string;
}

/** Failure describes terminal errors. */
export interface ComputeFailure {
  taskId: string;
  reason: string;
  details: { [key: string]: string };
  /** error taxonomy, remediation hints */
  metadata?: Metadata | undefined;
}

export interface ComputeFailure_DetailsEntry {
  key: string;
  value: string;
}

function createBaseDataRef(): DataRef {
  return { name: "", contentType: "", inlineBytes: undefined, inlineJson: undefined, uri: undefined, annotations: {} };
}

export const DataRef: MessageFns<DataRef> = {
  encode(message: DataRef, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.contentType !== "") {
      writer.uint32(18).string(message.contentType);
    }
    if (message.inlineBytes !== undefined) {
      writer.uint32(26).bytes(message.inlineBytes);
    }
    if (message.inlineJson !== undefined) {
      Struct.encode(Struct.wrap(message.inlineJson), writer.uint32(34).fork()).join();
    }
    if (message.uri !== undefined) {
      writer.uint32(42).string(message.uri);
    }
    Object.entries(message.annotations).forEach(([key, value]) => {
      DataRef_AnnotationsEntry.encode({ key: key as any, value }, writer.uint32(50).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DataRef {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDataRef();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.contentType = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.inlineBytes = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.inlineJson = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.uri = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          const entry6 = DataRef_AnnotationsEntry.decode(reader, reader.uint32());
          if (entry6.value !== undefined) {
            message.annotations[entry6.key] = entry6.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DataRef {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      contentType: isSet(object.contentType) ? globalThis.String(object.contentType) : "",
      inlineBytes: isSet(object.inlineBytes) ? bytesFromBase64(object.inlineBytes) : undefined,
      inlineJson: isObject(object.inlineJson) ? object.inlineJson : undefined,
      uri: isSet(object.uri) ? globalThis.String(object.uri) : undefined,
      annotations: isObject(object.annotations)
        ? Object.entries(object.annotations).reduce<{ [key: string]: string }>((acc, [key, value]) => {
          acc[key] = String(value);
          return acc;
        }, {})
        : {},
    };
  },

  toJSON(message: DataRef): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.contentType !== "") {
      obj.contentType = message.contentType;
    }
    if (message.inlineBytes !== undefined) {
      obj.inlineBytes = base64FromBytes(message.inlineBytes);
    }
    if (message.inlineJson !== undefined) {
      obj.inlineJson = message.inlineJson;
    }
    if (message.uri !== undefined) {
      obj.uri = message.uri;
    }
    if (message.annotations) {
      const entries = Object.entries(message.annotations);
      if (entries.length > 0) {
        obj.annotations = {};
        entries.forEach(([k, v]) => {
          obj.annotations[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DataRef>, I>>(base?: I): DataRef {
    return DataRef.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DataRef>, I>>(object: I): DataRef {
    const message = createBaseDataRef();
    message.name = object.name ?? "";
    message.contentType = object.contentType ?? "";
    message.inlineBytes = object.inlineBytes ?? undefined;
    message.inlineJson = object.inlineJson ?? undefined;
    message.uri = object.uri ?? undefined;
    message.annotations = Object.entries(object.annotations ?? {}).reduce<{ [key: string]: string }>(
      (acc, [key, value]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseDataRef_AnnotationsEntry(): DataRef_AnnotationsEntry {
  return { key: "", value: "" };
}

export const DataRef_AnnotationsEntry: MessageFns<DataRef_AnnotationsEntry> = {
  encode(message: DataRef_AnnotationsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DataRef_AnnotationsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDataRef_AnnotationsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DataRef_AnnotationsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: DataRef_AnnotationsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DataRef_AnnotationsEntry>, I>>(base?: I): DataRef_AnnotationsEntry {
    return DataRef_AnnotationsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DataRef_AnnotationsEntry>, I>>(object: I): DataRef_AnnotationsEntry {
    const message = createBaseDataRef_AnnotationsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseTensorSpec(): TensorSpec {
  return { dtype: "", shapes: [], layout: "", deviceHint: "", quant: undefined };
}

export const TensorSpec: MessageFns<TensorSpec> = {
  encode(message: TensorSpec, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.dtype !== "") {
      writer.uint32(10).string(message.dtype);
    }
    writer.uint32(18).fork();
    for (const v of message.shapes) {
      writer.int64(v);
    }
    writer.join();
    if (message.layout !== "") {
      writer.uint32(26).string(message.layout);
    }
    if (message.deviceHint !== "") {
      writer.uint32(34).string(message.deviceHint);
    }
    if (message.quant !== undefined) {
      TensorSpec_Quantization.encode(message.quant, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TensorSpec {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTensorSpec();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.dtype = reader.string();
          continue;
        }
        case 2: {
          if (tag === 16) {
            message.shapes.push(reader.int64().toString());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.shapes.push(reader.int64().toString());
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.layout = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.deviceHint = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.quant = TensorSpec_Quantization.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TensorSpec {
    return {
      dtype: isSet(object.dtype) ? globalThis.String(object.dtype) : "",
      shapes: globalThis.Array.isArray(object?.shapes) ? object.shapes.map((e: any) => globalThis.String(e)) : [],
      layout: isSet(object.layout) ? globalThis.String(object.layout) : "",
      deviceHint: isSet(object.deviceHint) ? globalThis.String(object.deviceHint) : "",
      quant: isSet(object.quant) ? TensorSpec_Quantization.fromJSON(object.quant) : undefined,
    };
  },

  toJSON(message: TensorSpec): unknown {
    const obj: any = {};
    if (message.dtype !== "") {
      obj.dtype = message.dtype;
    }
    if (message.shapes?.length) {
      obj.shapes = message.shapes;
    }
    if (message.layout !== "") {
      obj.layout = message.layout;
    }
    if (message.deviceHint !== "") {
      obj.deviceHint = message.deviceHint;
    }
    if (message.quant !== undefined) {
      obj.quant = TensorSpec_Quantization.toJSON(message.quant);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TensorSpec>, I>>(base?: I): TensorSpec {
    return TensorSpec.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TensorSpec>, I>>(object: I): TensorSpec {
    const message = createBaseTensorSpec();
    message.dtype = object.dtype ?? "";
    message.shapes = object.shapes?.map((e) => e) || [];
    message.layout = object.layout ?? "";
    message.deviceHint = object.deviceHint ?? "";
    message.quant = (object.quant !== undefined && object.quant !== null)
      ? TensorSpec_Quantization.fromPartial(object.quant)
      : undefined;
    return message;
  },
};

function createBaseTensorSpec_Quantization(): TensorSpec_Quantization {
  return { scale: 0, zeroPoint: "0", scheme: "" };
}

export const TensorSpec_Quantization: MessageFns<TensorSpec_Quantization> = {
  encode(message: TensorSpec_Quantization, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.scale !== 0) {
      writer.uint32(9).double(message.scale);
    }
    if (message.zeroPoint !== "0") {
      writer.uint32(16).int64(message.zeroPoint);
    }
    if (message.scheme !== "") {
      writer.uint32(26).string(message.scheme);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TensorSpec_Quantization {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTensorSpec_Quantization();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 9) {
            break;
          }

          message.scale = reader.double();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.zeroPoint = reader.int64().toString();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.scheme = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TensorSpec_Quantization {
    return {
      scale: isSet(object.scale) ? globalThis.Number(object.scale) : 0,
      zeroPoint: isSet(object.zeroPoint) ? globalThis.String(object.zeroPoint) : "0",
      scheme: isSet(object.scheme) ? globalThis.String(object.scheme) : "",
    };
  },

  toJSON(message: TensorSpec_Quantization): unknown {
    const obj: any = {};
    if (message.scale !== 0) {
      obj.scale = message.scale;
    }
    if (message.zeroPoint !== "0") {
      obj.zeroPoint = message.zeroPoint;
    }
    if (message.scheme !== "") {
      obj.scheme = message.scheme;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TensorSpec_Quantization>, I>>(base?: I): TensorSpec_Quantization {
    return TensorSpec_Quantization.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TensorSpec_Quantization>, I>>(object: I): TensorSpec_Quantization {
    const message = createBaseTensorSpec_Quantization();
    message.scale = object.scale ?? 0;
    message.zeroPoint = object.zeroPoint ?? "0";
    message.scheme = object.scheme ?? "";
    return message;
  },
};

function createBaseBufferSpec(): BufferSpec {
  return { byteLength: "0", stride: 0, offset: 0, alignment: 0 };
}

export const BufferSpec: MessageFns<BufferSpec> = {
  encode(message: BufferSpec, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.byteLength !== "0") {
      writer.uint32(8).uint64(message.byteLength);
    }
    if (message.stride !== 0) {
      writer.uint32(16).uint32(message.stride);
    }
    if (message.offset !== 0) {
      writer.uint32(24).uint32(message.offset);
    }
    if (message.alignment !== 0) {
      writer.uint32(32).uint32(message.alignment);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BufferSpec {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBufferSpec();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.byteLength = reader.uint64().toString();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.stride = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.offset = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.alignment = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BufferSpec {
    return {
      byteLength: isSet(object.byteLength) ? globalThis.String(object.byteLength) : "0",
      stride: isSet(object.stride) ? globalThis.Number(object.stride) : 0,
      offset: isSet(object.offset) ? globalThis.Number(object.offset) : 0,
      alignment: isSet(object.alignment) ? globalThis.Number(object.alignment) : 0,
    };
  },

  toJSON(message: BufferSpec): unknown {
    const obj: any = {};
    if (message.byteLength !== "0") {
      obj.byteLength = message.byteLength;
    }
    if (message.stride !== 0) {
      obj.stride = Math.round(message.stride);
    }
    if (message.offset !== 0) {
      obj.offset = Math.round(message.offset);
    }
    if (message.alignment !== 0) {
      obj.alignment = Math.round(message.alignment);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BufferSpec>, I>>(base?: I): BufferSpec {
    return BufferSpec.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BufferSpec>, I>>(object: I): BufferSpec {
    const message = createBaseBufferSpec();
    message.byteLength = object.byteLength ?? "0";
    message.stride = object.stride ?? 0;
    message.offset = object.offset ?? 0;
    message.alignment = object.alignment ?? 0;
    return message;
  },
};

function createBaseGPUDescriptor(): GPUDescriptor {
  return { backend: "", vendor: "", adapterName: "", adapterUuid: "", features: [], limits: {} };
}

export const GPUDescriptor: MessageFns<GPUDescriptor> = {
  encode(message: GPUDescriptor, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.backend !== "") {
      writer.uint32(10).string(message.backend);
    }
    if (message.vendor !== "") {
      writer.uint32(18).string(message.vendor);
    }
    if (message.adapterName !== "") {
      writer.uint32(26).string(message.adapterName);
    }
    if (message.adapterUuid !== "") {
      writer.uint32(34).string(message.adapterUuid);
    }
    for (const v of message.features) {
      writer.uint32(42).string(v!);
    }
    Object.entries(message.limits).forEach(([key, value]) => {
      GPUDescriptor_LimitsEntry.encode({ key: key as any, value }, writer.uint32(50).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GPUDescriptor {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGPUDescriptor();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.backend = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.vendor = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.adapterName = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.adapterUuid = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.features.push(reader.string());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          const entry6 = GPUDescriptor_LimitsEntry.decode(reader, reader.uint32());
          if (entry6.value !== undefined) {
            message.limits[entry6.key] = entry6.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GPUDescriptor {
    return {
      backend: isSet(object.backend) ? globalThis.String(object.backend) : "",
      vendor: isSet(object.vendor) ? globalThis.String(object.vendor) : "",
      adapterName: isSet(object.adapterName) ? globalThis.String(object.adapterName) : "",
      adapterUuid: isSet(object.adapterUuid) ? globalThis.String(object.adapterUuid) : "",
      features: globalThis.Array.isArray(object?.features) ? object.features.map((e: any) => globalThis.String(e)) : [],
      limits: isObject(object.limits)
        ? Object.entries(object.limits).reduce<{ [key: string]: string }>((acc, [key, value]) => {
          acc[key] = String(value);
          return acc;
        }, {})
        : {},
    };
  },

  toJSON(message: GPUDescriptor): unknown {
    const obj: any = {};
    if (message.backend !== "") {
      obj.backend = message.backend;
    }
    if (message.vendor !== "") {
      obj.vendor = message.vendor;
    }
    if (message.adapterName !== "") {
      obj.adapterName = message.adapterName;
    }
    if (message.adapterUuid !== "") {
      obj.adapterUuid = message.adapterUuid;
    }
    if (message.features?.length) {
      obj.features = message.features;
    }
    if (message.limits) {
      const entries = Object.entries(message.limits);
      if (entries.length > 0) {
        obj.limits = {};
        entries.forEach(([k, v]) => {
          obj.limits[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GPUDescriptor>, I>>(base?: I): GPUDescriptor {
    return GPUDescriptor.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GPUDescriptor>, I>>(object: I): GPUDescriptor {
    const message = createBaseGPUDescriptor();
    message.backend = object.backend ?? "";
    message.vendor = object.vendor ?? "";
    message.adapterName = object.adapterName ?? "";
    message.adapterUuid = object.adapterUuid ?? "";
    message.features = object.features?.map((e) => e) || [];
    message.limits = Object.entries(object.limits ?? {}).reduce<{ [key: string]: string }>((acc, [key, value]) => {
      if (value !== undefined) {
        acc[key] = globalThis.String(value);
      }
      return acc;
    }, {});
    return message;
  },
};

function createBaseGPUDescriptor_LimitsEntry(): GPUDescriptor_LimitsEntry {
  return { key: "", value: "0" };
}

export const GPUDescriptor_LimitsEntry: MessageFns<GPUDescriptor_LimitsEntry> = {
  encode(message: GPUDescriptor_LimitsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "0") {
      writer.uint32(16).uint64(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GPUDescriptor_LimitsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGPUDescriptor_LimitsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.value = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GPUDescriptor_LimitsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "0",
    };
  },

  toJSON(message: GPUDescriptor_LimitsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "0") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GPUDescriptor_LimitsEntry>, I>>(base?: I): GPUDescriptor_LimitsEntry {
    return GPUDescriptor_LimitsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GPUDescriptor_LimitsEntry>, I>>(object: I): GPUDescriptor_LimitsEntry {
    const message = createBaseGPUDescriptor_LimitsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "0";
    return message;
  },
};

function createBaseCapability(): Capability {
  return {
    wasm: false,
    threads: false,
    simd: false,
    webgpu: false,
    cpuCores: 0,
    memoryMb: 0,
    gpu: undefined,
    labels: [],
    attributes: {},
    metadata: undefined,
  };
}

export const Capability: MessageFns<Capability> = {
  encode(message: Capability, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.wasm !== false) {
      writer.uint32(8).bool(message.wasm);
    }
    if (message.threads !== false) {
      writer.uint32(16).bool(message.threads);
    }
    if (message.simd !== false) {
      writer.uint32(24).bool(message.simd);
    }
    if (message.webgpu !== false) {
      writer.uint32(32).bool(message.webgpu);
    }
    if (message.cpuCores !== 0) {
      writer.uint32(40).uint32(message.cpuCores);
    }
    if (message.memoryMb !== 0) {
      writer.uint32(48).uint32(message.memoryMb);
    }
    if (message.gpu !== undefined) {
      GPUDescriptor.encode(message.gpu, writer.uint32(58).fork()).join();
    }
    for (const v of message.labels) {
      writer.uint32(66).string(v!);
    }
    Object.entries(message.attributes).forEach(([key, value]) => {
      Capability_AttributesEntry.encode({ key: key as any, value }, writer.uint32(74).fork()).join();
    });
    if (message.metadata !== undefined) {
      Metadata.encode(message.metadata, writer.uint32(82).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Capability {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCapability();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.wasm = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.threads = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.simd = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.webgpu = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.cpuCores = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.memoryMb = reader.uint32();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.gpu = GPUDescriptor.decode(reader, reader.uint32());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.labels.push(reader.string());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          const entry9 = Capability_AttributesEntry.decode(reader, reader.uint32());
          if (entry9.value !== undefined) {
            message.attributes[entry9.key] = entry9.value;
          }
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.metadata = Metadata.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Capability {
    return {
      wasm: isSet(object.wasm) ? globalThis.Boolean(object.wasm) : false,
      threads: isSet(object.threads) ? globalThis.Boolean(object.threads) : false,
      simd: isSet(object.simd) ? globalThis.Boolean(object.simd) : false,
      webgpu: isSet(object.webgpu) ? globalThis.Boolean(object.webgpu) : false,
      cpuCores: isSet(object.cpuCores) ? globalThis.Number(object.cpuCores) : 0,
      memoryMb: isSet(object.memoryMb) ? globalThis.Number(object.memoryMb) : 0,
      gpu: isSet(object.gpu) ? GPUDescriptor.fromJSON(object.gpu) : undefined,
      labels: globalThis.Array.isArray(object?.labels) ? object.labels.map((e: any) => globalThis.String(e)) : [],
      attributes: isObject(object.attributes)
        ? Object.entries(object.attributes).reduce<{ [key: string]: string }>((acc, [key, value]) => {
          acc[key] = String(value);
          return acc;
        }, {})
        : {},
      metadata: isSet(object.metadata) ? Metadata.fromJSON(object.metadata) : undefined,
    };
  },

  toJSON(message: Capability): unknown {
    const obj: any = {};
    if (message.wasm !== false) {
      obj.wasm = message.wasm;
    }
    if (message.threads !== false) {
      obj.threads = message.threads;
    }
    if (message.simd !== false) {
      obj.simd = message.simd;
    }
    if (message.webgpu !== false) {
      obj.webgpu = message.webgpu;
    }
    if (message.cpuCores !== 0) {
      obj.cpuCores = Math.round(message.cpuCores);
    }
    if (message.memoryMb !== 0) {
      obj.memoryMb = Math.round(message.memoryMb);
    }
    if (message.gpu !== undefined) {
      obj.gpu = GPUDescriptor.toJSON(message.gpu);
    }
    if (message.labels?.length) {
      obj.labels = message.labels;
    }
    if (message.attributes) {
      const entries = Object.entries(message.attributes);
      if (entries.length > 0) {
        obj.attributes = {};
        entries.forEach(([k, v]) => {
          obj.attributes[k] = v;
        });
      }
    }
    if (message.metadata !== undefined) {
      obj.metadata = Metadata.toJSON(message.metadata);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Capability>, I>>(base?: I): Capability {
    return Capability.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Capability>, I>>(object: I): Capability {
    const message = createBaseCapability();
    message.wasm = object.wasm ?? false;
    message.threads = object.threads ?? false;
    message.simd = object.simd ?? false;
    message.webgpu = object.webgpu ?? false;
    message.cpuCores = object.cpuCores ?? 0;
    message.memoryMb = object.memoryMb ?? 0;
    message.gpu = (object.gpu !== undefined && object.gpu !== null) ? GPUDescriptor.fromPartial(object.gpu) : undefined;
    message.labels = object.labels?.map((e) => e) || [];
    message.attributes = Object.entries(object.attributes ?? {}).reduce<{ [key: string]: string }>(
      (acc, [key, value]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.metadata = (object.metadata !== undefined && object.metadata !== null)
      ? Metadata.fromPartial(object.metadata)
      : undefined;
    return message;
  },
};

function createBaseCapability_AttributesEntry(): Capability_AttributesEntry {
  return { key: "", value: "" };
}

export const Capability_AttributesEntry: MessageFns<Capability_AttributesEntry> = {
  encode(message: Capability_AttributesEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Capability_AttributesEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCapability_AttributesEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Capability_AttributesEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: Capability_AttributesEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Capability_AttributesEntry>, I>>(base?: I): Capability_AttributesEntry {
    return Capability_AttributesEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Capability_AttributesEntry>, I>>(object: I): Capability_AttributesEntry {
    const message = createBaseCapability_AttributesEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseRequirements(): Requirements {
  return {
    min: undefined,
    preferred: undefined,
    qos: {},
    isolation: "",
    locality: {},
    metadata: undefined,
    parallelism: undefined,
  };
}

export const Requirements: MessageFns<Requirements> = {
  encode(message: Requirements, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.min !== undefined) {
      Capability.encode(message.min, writer.uint32(10).fork()).join();
    }
    if (message.preferred !== undefined) {
      Capability.encode(message.preferred, writer.uint32(18).fork()).join();
    }
    Object.entries(message.qos).forEach(([key, value]) => {
      Requirements_QosEntry.encode({ key: key as any, value }, writer.uint32(26).fork()).join();
    });
    if (message.isolation !== "") {
      writer.uint32(34).string(message.isolation);
    }
    Object.entries(message.locality).forEach(([key, value]) => {
      Requirements_LocalityEntry.encode({ key: key as any, value }, writer.uint32(42).fork()).join();
    });
    if (message.metadata !== undefined) {
      Metadata.encode(message.metadata, writer.uint32(50).fork()).join();
    }
    if (message.parallelism !== undefined) {
      ParallelismConfig.encode(message.parallelism, writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Requirements {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequirements();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.min = Capability.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.preferred = Capability.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const entry3 = Requirements_QosEntry.decode(reader, reader.uint32());
          if (entry3.value !== undefined) {
            message.qos[entry3.key] = entry3.value;
          }
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.isolation = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          const entry5 = Requirements_LocalityEntry.decode(reader, reader.uint32());
          if (entry5.value !== undefined) {
            message.locality[entry5.key] = entry5.value;
          }
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.metadata = Metadata.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.parallelism = ParallelismConfig.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Requirements {
    return {
      min: isSet(object.min) ? Capability.fromJSON(object.min) : undefined,
      preferred: isSet(object.preferred) ? Capability.fromJSON(object.preferred) : undefined,
      qos: isObject(object.qos)
        ? Object.entries(object.qos).reduce<{ [key: string]: string }>((acc, [key, value]) => {
          acc[key] = String(value);
          return acc;
        }, {})
        : {},
      isolation: isSet(object.isolation) ? globalThis.String(object.isolation) : "",
      locality: isObject(object.locality)
        ? Object.entries(object.locality).reduce<{ [key: string]: string }>((acc, [key, value]) => {
          acc[key] = String(value);
          return acc;
        }, {})
        : {},
      metadata: isSet(object.metadata) ? Metadata.fromJSON(object.metadata) : undefined,
      parallelism: isSet(object.parallelism) ? ParallelismConfig.fromJSON(object.parallelism) : undefined,
    };
  },

  toJSON(message: Requirements): unknown {
    const obj: any = {};
    if (message.min !== undefined) {
      obj.min = Capability.toJSON(message.min);
    }
    if (message.preferred !== undefined) {
      obj.preferred = Capability.toJSON(message.preferred);
    }
    if (message.qos) {
      const entries = Object.entries(message.qos);
      if (entries.length > 0) {
        obj.qos = {};
        entries.forEach(([k, v]) => {
          obj.qos[k] = v;
        });
      }
    }
    if (message.isolation !== "") {
      obj.isolation = message.isolation;
    }
    if (message.locality) {
      const entries = Object.entries(message.locality);
      if (entries.length > 0) {
        obj.locality = {};
        entries.forEach(([k, v]) => {
          obj.locality[k] = v;
        });
      }
    }
    if (message.metadata !== undefined) {
      obj.metadata = Metadata.toJSON(message.metadata);
    }
    if (message.parallelism !== undefined) {
      obj.parallelism = ParallelismConfig.toJSON(message.parallelism);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Requirements>, I>>(base?: I): Requirements {
    return Requirements.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Requirements>, I>>(object: I): Requirements {
    const message = createBaseRequirements();
    message.min = (object.min !== undefined && object.min !== null) ? Capability.fromPartial(object.min) : undefined;
    message.preferred = (object.preferred !== undefined && object.preferred !== null)
      ? Capability.fromPartial(object.preferred)
      : undefined;
    message.qos = Object.entries(object.qos ?? {}).reduce<{ [key: string]: string }>((acc, [key, value]) => {
      if (value !== undefined) {
        acc[key] = globalThis.String(value);
      }
      return acc;
    }, {});
    message.isolation = object.isolation ?? "";
    message.locality = Object.entries(object.locality ?? {}).reduce<{ [key: string]: string }>((acc, [key, value]) => {
      if (value !== undefined) {
        acc[key] = globalThis.String(value);
      }
      return acc;
    }, {});
    message.metadata = (object.metadata !== undefined && object.metadata !== null)
      ? Metadata.fromPartial(object.metadata)
      : undefined;
    message.parallelism = (object.parallelism !== undefined && object.parallelism !== null)
      ? ParallelismConfig.fromPartial(object.parallelism)
      : undefined;
    return message;
  },
};

function createBaseRequirements_QosEntry(): Requirements_QosEntry {
  return { key: "", value: "" };
}

export const Requirements_QosEntry: MessageFns<Requirements_QosEntry> = {
  encode(message: Requirements_QosEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Requirements_QosEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequirements_QosEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Requirements_QosEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: Requirements_QosEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Requirements_QosEntry>, I>>(base?: I): Requirements_QosEntry {
    return Requirements_QosEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Requirements_QosEntry>, I>>(object: I): Requirements_QosEntry {
    const message = createBaseRequirements_QosEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseRequirements_LocalityEntry(): Requirements_LocalityEntry {
  return { key: "", value: "" };
}

export const Requirements_LocalityEntry: MessageFns<Requirements_LocalityEntry> = {
  encode(message: Requirements_LocalityEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Requirements_LocalityEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequirements_LocalityEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Requirements_LocalityEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: Requirements_LocalityEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Requirements_LocalityEntry>, I>>(base?: I): Requirements_LocalityEntry {
    return Requirements_LocalityEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Requirements_LocalityEntry>, I>>(object: I): Requirements_LocalityEntry {
    const message = createBaseRequirements_LocalityEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseParallelismConfig(): ParallelismConfig {
  return { strategy: "", maxChunks: 0 };
}

export const ParallelismConfig: MessageFns<ParallelismConfig> = {
  encode(message: ParallelismConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.strategy !== "") {
      writer.uint32(10).string(message.strategy);
    }
    if (message.maxChunks !== 0) {
      writer.uint32(16).uint32(message.maxChunks);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ParallelismConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseParallelismConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.strategy = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.maxChunks = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ParallelismConfig {
    return {
      strategy: isSet(object.strategy) ? globalThis.String(object.strategy) : "",
      maxChunks: isSet(object.maxChunks) ? globalThis.Number(object.maxChunks) : 0,
    };
  },

  toJSON(message: ParallelismConfig): unknown {
    const obj: any = {};
    if (message.strategy !== "") {
      obj.strategy = message.strategy;
    }
    if (message.maxChunks !== 0) {
      obj.maxChunks = Math.round(message.maxChunks);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ParallelismConfig>, I>>(base?: I): ParallelismConfig {
    return ParallelismConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ParallelismConfig>, I>>(object: I): ParallelismConfig {
    const message = createBaseParallelismConfig();
    message.strategy = object.strategy ?? "";
    message.maxChunks = object.maxChunks ?? 0;
    return message;
  },
};

function createBaseModuleSpec(): ModuleSpec {
  return {
    kind: "",
    uri: "",
    hash: "",
    entry: "",
    expectedInputs: [],
    expectedOutputs: [],
    params: {},
    permissions: {},
    metadata: undefined,
  };
}

export const ModuleSpec: MessageFns<ModuleSpec> = {
  encode(message: ModuleSpec, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.kind !== "") {
      writer.uint32(10).string(message.kind);
    }
    if (message.uri !== "") {
      writer.uint32(18).string(message.uri);
    }
    if (message.hash !== "") {
      writer.uint32(26).string(message.hash);
    }
    if (message.entry !== "") {
      writer.uint32(34).string(message.entry);
    }
    for (const v of message.expectedInputs) {
      TensorSpec.encode(v!, writer.uint32(42).fork()).join();
    }
    for (const v of message.expectedOutputs) {
      TensorSpec.encode(v!, writer.uint32(50).fork()).join();
    }
    Object.entries(message.params).forEach(([key, value]) => {
      ModuleSpec_ParamsEntry.encode({ key: key as any, value }, writer.uint32(58).fork()).join();
    });
    Object.entries(message.permissions).forEach(([key, value]) => {
      ModuleSpec_PermissionsEntry.encode({ key: key as any, value }, writer.uint32(66).fork()).join();
    });
    if (message.metadata !== undefined) {
      Metadata.encode(message.metadata, writer.uint32(74).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ModuleSpec {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModuleSpec();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.kind = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.uri = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.hash = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.entry = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.expectedInputs.push(TensorSpec.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.expectedOutputs.push(TensorSpec.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          const entry7 = ModuleSpec_ParamsEntry.decode(reader, reader.uint32());
          if (entry7.value !== undefined) {
            message.params[entry7.key] = entry7.value;
          }
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          const entry8 = ModuleSpec_PermissionsEntry.decode(reader, reader.uint32());
          if (entry8.value !== undefined) {
            message.permissions[entry8.key] = entry8.value;
          }
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.metadata = Metadata.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModuleSpec {
    return {
      kind: isSet(object.kind) ? globalThis.String(object.kind) : "",
      uri: isSet(object.uri) ? globalThis.String(object.uri) : "",
      hash: isSet(object.hash) ? globalThis.String(object.hash) : "",
      entry: isSet(object.entry) ? globalThis.String(object.entry) : "",
      expectedInputs: globalThis.Array.isArray(object?.expectedInputs)
        ? object.expectedInputs.map((e: any) => TensorSpec.fromJSON(e))
        : [],
      expectedOutputs: globalThis.Array.isArray(object?.expectedOutputs)
        ? object.expectedOutputs.map((e: any) => TensorSpec.fromJSON(e))
        : [],
      params: isObject(object.params)
        ? Object.entries(object.params).reduce<{ [key: string]: string }>((acc, [key, value]) => {
          acc[key] = String(value);
          return acc;
        }, {})
        : {},
      permissions: isObject(object.permissions)
        ? Object.entries(object.permissions).reduce<{ [key: string]: string }>((acc, [key, value]) => {
          acc[key] = String(value);
          return acc;
        }, {})
        : {},
      metadata: isSet(object.metadata) ? Metadata.fromJSON(object.metadata) : undefined,
    };
  },

  toJSON(message: ModuleSpec): unknown {
    const obj: any = {};
    if (message.kind !== "") {
      obj.kind = message.kind;
    }
    if (message.uri !== "") {
      obj.uri = message.uri;
    }
    if (message.hash !== "") {
      obj.hash = message.hash;
    }
    if (message.entry !== "") {
      obj.entry = message.entry;
    }
    if (message.expectedInputs?.length) {
      obj.expectedInputs = message.expectedInputs.map((e) => TensorSpec.toJSON(e));
    }
    if (message.expectedOutputs?.length) {
      obj.expectedOutputs = message.expectedOutputs.map((e) => TensorSpec.toJSON(e));
    }
    if (message.params) {
      const entries = Object.entries(message.params);
      if (entries.length > 0) {
        obj.params = {};
        entries.forEach(([k, v]) => {
          obj.params[k] = v;
        });
      }
    }
    if (message.permissions) {
      const entries = Object.entries(message.permissions);
      if (entries.length > 0) {
        obj.permissions = {};
        entries.forEach(([k, v]) => {
          obj.permissions[k] = v;
        });
      }
    }
    if (message.metadata !== undefined) {
      obj.metadata = Metadata.toJSON(message.metadata);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModuleSpec>, I>>(base?: I): ModuleSpec {
    return ModuleSpec.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModuleSpec>, I>>(object: I): ModuleSpec {
    const message = createBaseModuleSpec();
    message.kind = object.kind ?? "";
    message.uri = object.uri ?? "";
    message.hash = object.hash ?? "";
    message.entry = object.entry ?? "";
    message.expectedInputs = object.expectedInputs?.map((e) => TensorSpec.fromPartial(e)) || [];
    message.expectedOutputs = object.expectedOutputs?.map((e) => TensorSpec.fromPartial(e)) || [];
    message.params = Object.entries(object.params ?? {}).reduce<{ [key: string]: string }>((acc, [key, value]) => {
      if (value !== undefined) {
        acc[key] = globalThis.String(value);
      }
      return acc;
    }, {});
    message.permissions = Object.entries(object.permissions ?? {}).reduce<{ [key: string]: string }>(
      (acc, [key, value]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.metadata = (object.metadata !== undefined && object.metadata !== null)
      ? Metadata.fromPartial(object.metadata)
      : undefined;
    return message;
  },
};

function createBaseModuleSpec_ParamsEntry(): ModuleSpec_ParamsEntry {
  return { key: "", value: "" };
}

export const ModuleSpec_ParamsEntry: MessageFns<ModuleSpec_ParamsEntry> = {
  encode(message: ModuleSpec_ParamsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ModuleSpec_ParamsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModuleSpec_ParamsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModuleSpec_ParamsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: ModuleSpec_ParamsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModuleSpec_ParamsEntry>, I>>(base?: I): ModuleSpec_ParamsEntry {
    return ModuleSpec_ParamsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModuleSpec_ParamsEntry>, I>>(object: I): ModuleSpec_ParamsEntry {
    const message = createBaseModuleSpec_ParamsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseModuleSpec_PermissionsEntry(): ModuleSpec_PermissionsEntry {
  return { key: "", value: "" };
}

export const ModuleSpec_PermissionsEntry: MessageFns<ModuleSpec_PermissionsEntry> = {
  encode(message: ModuleSpec_PermissionsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ModuleSpec_PermissionsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseModuleSpec_PermissionsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ModuleSpec_PermissionsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: ModuleSpec_PermissionsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ModuleSpec_PermissionsEntry>, I>>(base?: I): ModuleSpec_PermissionsEntry {
    return ModuleSpec_PermissionsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModuleSpec_PermissionsEntry>, I>>(object: I): ModuleSpec_PermissionsEntry {
    const message = createBaseModuleSpec_PermissionsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseComputeEnvelope(): ComputeEnvelope {
  return {
    taskId: "",
    op: "",
    version: "",
    module: undefined,
    requirements: undefined,
    inputs: [],
    params: {},
    returnChannel: "",
    security: {},
    notBefore: undefined,
    deadline: undefined,
    metadata: undefined,
  };
}

export const ComputeEnvelope: MessageFns<ComputeEnvelope> = {
  encode(message: ComputeEnvelope, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.taskId !== "") {
      writer.uint32(10).string(message.taskId);
    }
    if (message.op !== "") {
      writer.uint32(18).string(message.op);
    }
    if (message.version !== "") {
      writer.uint32(26).string(message.version);
    }
    if (message.module !== undefined) {
      ModuleSpec.encode(message.module, writer.uint32(34).fork()).join();
    }
    if (message.requirements !== undefined) {
      Requirements.encode(message.requirements, writer.uint32(42).fork()).join();
    }
    for (const v of message.inputs) {
      DataRef.encode(v!, writer.uint32(50).fork()).join();
    }
    Object.entries(message.params).forEach(([key, value]) => {
      ComputeEnvelope_ParamsEntry.encode({ key: key as any, value }, writer.uint32(58).fork()).join();
    });
    if (message.returnChannel !== "") {
      writer.uint32(66).string(message.returnChannel);
    }
    Object.entries(message.security).forEach(([key, value]) => {
      ComputeEnvelope_SecurityEntry.encode({ key: key as any, value }, writer.uint32(74).fork()).join();
    });
    if (message.notBefore !== undefined) {
      Timestamp.encode(toTimestamp(message.notBefore), writer.uint32(82).fork()).join();
    }
    if (message.deadline !== undefined) {
      Timestamp.encode(toTimestamp(message.deadline), writer.uint32(90).fork()).join();
    }
    if (message.metadata !== undefined) {
      Metadata.encode(message.metadata, writer.uint32(98).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ComputeEnvelope {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseComputeEnvelope();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.taskId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.op = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.module = ModuleSpec.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.requirements = Requirements.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.inputs.push(DataRef.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          const entry7 = ComputeEnvelope_ParamsEntry.decode(reader, reader.uint32());
          if (entry7.value !== undefined) {
            message.params[entry7.key] = entry7.value;
          }
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.returnChannel = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          const entry9 = ComputeEnvelope_SecurityEntry.decode(reader, reader.uint32());
          if (entry9.value !== undefined) {
            message.security[entry9.key] = entry9.value;
          }
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.notBefore = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.deadline = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.metadata = Metadata.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ComputeEnvelope {
    return {
      taskId: isSet(object.taskId) ? globalThis.String(object.taskId) : "",
      op: isSet(object.op) ? globalThis.String(object.op) : "",
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      module: isSet(object.module) ? ModuleSpec.fromJSON(object.module) : undefined,
      requirements: isSet(object.requirements) ? Requirements.fromJSON(object.requirements) : undefined,
      inputs: globalThis.Array.isArray(object?.inputs) ? object.inputs.map((e: any) => DataRef.fromJSON(e)) : [],
      params: isObject(object.params)
        ? Object.entries(object.params).reduce<{ [key: string]: string }>((acc, [key, value]) => {
          acc[key] = String(value);
          return acc;
        }, {})
        : {},
      returnChannel: isSet(object.returnChannel) ? globalThis.String(object.returnChannel) : "",
      security: isObject(object.security)
        ? Object.entries(object.security).reduce<{ [key: string]: string }>((acc, [key, value]) => {
          acc[key] = String(value);
          return acc;
        }, {})
        : {},
      notBefore: isSet(object.notBefore) ? fromJsonTimestamp(object.notBefore) : undefined,
      deadline: isSet(object.deadline) ? fromJsonTimestamp(object.deadline) : undefined,
      metadata: isSet(object.metadata) ? Metadata.fromJSON(object.metadata) : undefined,
    };
  },

  toJSON(message: ComputeEnvelope): unknown {
    const obj: any = {};
    if (message.taskId !== "") {
      obj.taskId = message.taskId;
    }
    if (message.op !== "") {
      obj.op = message.op;
    }
    if (message.version !== "") {
      obj.version = message.version;
    }
    if (message.module !== undefined) {
      obj.module = ModuleSpec.toJSON(message.module);
    }
    if (message.requirements !== undefined) {
      obj.requirements = Requirements.toJSON(message.requirements);
    }
    if (message.inputs?.length) {
      obj.inputs = message.inputs.map((e) => DataRef.toJSON(e));
    }
    if (message.params) {
      const entries = Object.entries(message.params);
      if (entries.length > 0) {
        obj.params = {};
        entries.forEach(([k, v]) => {
          obj.params[k] = v;
        });
      }
    }
    if (message.returnChannel !== "") {
      obj.returnChannel = message.returnChannel;
    }
    if (message.security) {
      const entries = Object.entries(message.security);
      if (entries.length > 0) {
        obj.security = {};
        entries.forEach(([k, v]) => {
          obj.security[k] = v;
        });
      }
    }
    if (message.notBefore !== undefined) {
      obj.notBefore = message.notBefore.toISOString();
    }
    if (message.deadline !== undefined) {
      obj.deadline = message.deadline.toISOString();
    }
    if (message.metadata !== undefined) {
      obj.metadata = Metadata.toJSON(message.metadata);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ComputeEnvelope>, I>>(base?: I): ComputeEnvelope {
    return ComputeEnvelope.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ComputeEnvelope>, I>>(object: I): ComputeEnvelope {
    const message = createBaseComputeEnvelope();
    message.taskId = object.taskId ?? "";
    message.op = object.op ?? "";
    message.version = object.version ?? "";
    message.module = (object.module !== undefined && object.module !== null)
      ? ModuleSpec.fromPartial(object.module)
      : undefined;
    message.requirements = (object.requirements !== undefined && object.requirements !== null)
      ? Requirements.fromPartial(object.requirements)
      : undefined;
    message.inputs = object.inputs?.map((e) => DataRef.fromPartial(e)) || [];
    message.params = Object.entries(object.params ?? {}).reduce<{ [key: string]: string }>((acc, [key, value]) => {
      if (value !== undefined) {
        acc[key] = globalThis.String(value);
      }
      return acc;
    }, {});
    message.returnChannel = object.returnChannel ?? "";
    message.security = Object.entries(object.security ?? {}).reduce<{ [key: string]: string }>((acc, [key, value]) => {
      if (value !== undefined) {
        acc[key] = globalThis.String(value);
      }
      return acc;
    }, {});
    message.notBefore = object.notBefore ?? undefined;
    message.deadline = object.deadline ?? undefined;
    message.metadata = (object.metadata !== undefined && object.metadata !== null)
      ? Metadata.fromPartial(object.metadata)
      : undefined;
    return message;
  },
};

function createBaseComputeEnvelope_ParamsEntry(): ComputeEnvelope_ParamsEntry {
  return { key: "", value: "" };
}

export const ComputeEnvelope_ParamsEntry: MessageFns<ComputeEnvelope_ParamsEntry> = {
  encode(message: ComputeEnvelope_ParamsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ComputeEnvelope_ParamsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseComputeEnvelope_ParamsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ComputeEnvelope_ParamsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: ComputeEnvelope_ParamsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ComputeEnvelope_ParamsEntry>, I>>(base?: I): ComputeEnvelope_ParamsEntry {
    return ComputeEnvelope_ParamsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ComputeEnvelope_ParamsEntry>, I>>(object: I): ComputeEnvelope_ParamsEntry {
    const message = createBaseComputeEnvelope_ParamsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseComputeEnvelope_SecurityEntry(): ComputeEnvelope_SecurityEntry {
  return { key: "", value: "" };
}

export const ComputeEnvelope_SecurityEntry: MessageFns<ComputeEnvelope_SecurityEntry> = {
  encode(message: ComputeEnvelope_SecurityEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ComputeEnvelope_SecurityEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseComputeEnvelope_SecurityEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ComputeEnvelope_SecurityEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: ComputeEnvelope_SecurityEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ComputeEnvelope_SecurityEntry>, I>>(base?: I): ComputeEnvelope_SecurityEntry {
    return ComputeEnvelope_SecurityEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ComputeEnvelope_SecurityEntry>, I>>(
    object: I,
  ): ComputeEnvelope_SecurityEntry {
    const message = createBaseComputeEnvelope_SecurityEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseComputeClaim(): ComputeClaim {
  return { taskId: "", workerId: "", capabilities: undefined, metadata: undefined };
}

export const ComputeClaim: MessageFns<ComputeClaim> = {
  encode(message: ComputeClaim, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.taskId !== "") {
      writer.uint32(10).string(message.taskId);
    }
    if (message.workerId !== "") {
      writer.uint32(18).string(message.workerId);
    }
    if (message.capabilities !== undefined) {
      Capability.encode(message.capabilities, writer.uint32(26).fork()).join();
    }
    if (message.metadata !== undefined) {
      Metadata.encode(message.metadata, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ComputeClaim {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseComputeClaim();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.taskId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.workerId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.capabilities = Capability.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.metadata = Metadata.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ComputeClaim {
    return {
      taskId: isSet(object.taskId) ? globalThis.String(object.taskId) : "",
      workerId: isSet(object.workerId) ? globalThis.String(object.workerId) : "",
      capabilities: isSet(object.capabilities) ? Capability.fromJSON(object.capabilities) : undefined,
      metadata: isSet(object.metadata) ? Metadata.fromJSON(object.metadata) : undefined,
    };
  },

  toJSON(message: ComputeClaim): unknown {
    const obj: any = {};
    if (message.taskId !== "") {
      obj.taskId = message.taskId;
    }
    if (message.workerId !== "") {
      obj.workerId = message.workerId;
    }
    if (message.capabilities !== undefined) {
      obj.capabilities = Capability.toJSON(message.capabilities);
    }
    if (message.metadata !== undefined) {
      obj.metadata = Metadata.toJSON(message.metadata);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ComputeClaim>, I>>(base?: I): ComputeClaim {
    return ComputeClaim.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ComputeClaim>, I>>(object: I): ComputeClaim {
    const message = createBaseComputeClaim();
    message.taskId = object.taskId ?? "";
    message.workerId = object.workerId ?? "";
    message.capabilities = (object.capabilities !== undefined && object.capabilities !== null)
      ? Capability.fromPartial(object.capabilities)
      : undefined;
    message.metadata = (object.metadata !== undefined && object.metadata !== null)
      ? Metadata.fromPartial(object.metadata)
      : undefined;
    return message;
  },
};

function createBaseComputeAssignment(): ComputeAssignment {
  return { taskId: "", workerId: "", metadata: {}, meta: undefined };
}

export const ComputeAssignment: MessageFns<ComputeAssignment> = {
  encode(message: ComputeAssignment, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.taskId !== "") {
      writer.uint32(10).string(message.taskId);
    }
    if (message.workerId !== "") {
      writer.uint32(18).string(message.workerId);
    }
    Object.entries(message.metadata).forEach(([key, value]) => {
      ComputeAssignment_MetadataEntry.encode({ key: key as any, value }, writer.uint32(26).fork()).join();
    });
    if (message.meta !== undefined) {
      Metadata.encode(message.meta, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ComputeAssignment {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseComputeAssignment();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.taskId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.workerId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const entry3 = ComputeAssignment_MetadataEntry.decode(reader, reader.uint32());
          if (entry3.value !== undefined) {
            message.metadata[entry3.key] = entry3.value;
          }
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.meta = Metadata.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ComputeAssignment {
    return {
      taskId: isSet(object.taskId) ? globalThis.String(object.taskId) : "",
      workerId: isSet(object.workerId) ? globalThis.String(object.workerId) : "",
      metadata: isObject(object.metadata)
        ? Object.entries(object.metadata).reduce<{ [key: string]: string }>((acc, [key, value]) => {
          acc[key] = String(value);
          return acc;
        }, {})
        : {},
      meta: isSet(object.meta) ? Metadata.fromJSON(object.meta) : undefined,
    };
  },

  toJSON(message: ComputeAssignment): unknown {
    const obj: any = {};
    if (message.taskId !== "") {
      obj.taskId = message.taskId;
    }
    if (message.workerId !== "") {
      obj.workerId = message.workerId;
    }
    if (message.metadata) {
      const entries = Object.entries(message.metadata);
      if (entries.length > 0) {
        obj.metadata = {};
        entries.forEach(([k, v]) => {
          obj.metadata[k] = v;
        });
      }
    }
    if (message.meta !== undefined) {
      obj.meta = Metadata.toJSON(message.meta);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ComputeAssignment>, I>>(base?: I): ComputeAssignment {
    return ComputeAssignment.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ComputeAssignment>, I>>(object: I): ComputeAssignment {
    const message = createBaseComputeAssignment();
    message.taskId = object.taskId ?? "";
    message.workerId = object.workerId ?? "";
    message.metadata = Object.entries(object.metadata ?? {}).reduce<{ [key: string]: string }>((acc, [key, value]) => {
      if (value !== undefined) {
        acc[key] = globalThis.String(value);
      }
      return acc;
    }, {});
    message.meta = (object.meta !== undefined && object.meta !== null) ? Metadata.fromPartial(object.meta) : undefined;
    return message;
  },
};

function createBaseComputeAssignment_MetadataEntry(): ComputeAssignment_MetadataEntry {
  return { key: "", value: "" };
}

export const ComputeAssignment_MetadataEntry: MessageFns<ComputeAssignment_MetadataEntry> = {
  encode(message: ComputeAssignment_MetadataEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ComputeAssignment_MetadataEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseComputeAssignment_MetadataEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ComputeAssignment_MetadataEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: ComputeAssignment_MetadataEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ComputeAssignment_MetadataEntry>, I>>(base?: I): ComputeAssignment_MetadataEntry {
    return ComputeAssignment_MetadataEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ComputeAssignment_MetadataEntry>, I>>(
    object: I,
  ): ComputeAssignment_MetadataEntry {
    const message = createBaseComputeAssignment_MetadataEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseComputeProgress(): ComputeProgress {
  return { taskId: "", pct: 0, note: "", metrics: {}, metadata: undefined };
}

export const ComputeProgress: MessageFns<ComputeProgress> = {
  encode(message: ComputeProgress, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.taskId !== "") {
      writer.uint32(10).string(message.taskId);
    }
    if (message.pct !== 0) {
      writer.uint32(16).uint32(message.pct);
    }
    if (message.note !== "") {
      writer.uint32(26).string(message.note);
    }
    Object.entries(message.metrics).forEach(([key, value]) => {
      ComputeProgress_MetricsEntry.encode({ key: key as any, value }, writer.uint32(34).fork()).join();
    });
    if (message.metadata !== undefined) {
      Metadata.encode(message.metadata, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ComputeProgress {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseComputeProgress();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.taskId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.pct = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.note = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          const entry4 = ComputeProgress_MetricsEntry.decode(reader, reader.uint32());
          if (entry4.value !== undefined) {
            message.metrics[entry4.key] = entry4.value;
          }
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.metadata = Metadata.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ComputeProgress {
    return {
      taskId: isSet(object.taskId) ? globalThis.String(object.taskId) : "",
      pct: isSet(object.pct) ? globalThis.Number(object.pct) : 0,
      note: isSet(object.note) ? globalThis.String(object.note) : "",
      metrics: isObject(object.metrics)
        ? Object.entries(object.metrics).reduce<{ [key: string]: string }>((acc, [key, value]) => {
          acc[key] = String(value);
          return acc;
        }, {})
        : {},
      metadata: isSet(object.metadata) ? Metadata.fromJSON(object.metadata) : undefined,
    };
  },

  toJSON(message: ComputeProgress): unknown {
    const obj: any = {};
    if (message.taskId !== "") {
      obj.taskId = message.taskId;
    }
    if (message.pct !== 0) {
      obj.pct = Math.round(message.pct);
    }
    if (message.note !== "") {
      obj.note = message.note;
    }
    if (message.metrics) {
      const entries = Object.entries(message.metrics);
      if (entries.length > 0) {
        obj.metrics = {};
        entries.forEach(([k, v]) => {
          obj.metrics[k] = v;
        });
      }
    }
    if (message.metadata !== undefined) {
      obj.metadata = Metadata.toJSON(message.metadata);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ComputeProgress>, I>>(base?: I): ComputeProgress {
    return ComputeProgress.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ComputeProgress>, I>>(object: I): ComputeProgress {
    const message = createBaseComputeProgress();
    message.taskId = object.taskId ?? "";
    message.pct = object.pct ?? 0;
    message.note = object.note ?? "";
    message.metrics = Object.entries(object.metrics ?? {}).reduce<{ [key: string]: string }>((acc, [key, value]) => {
      if (value !== undefined) {
        acc[key] = globalThis.String(value);
      }
      return acc;
    }, {});
    message.metadata = (object.metadata !== undefined && object.metadata !== null)
      ? Metadata.fromPartial(object.metadata)
      : undefined;
    return message;
  },
};

function createBaseComputeProgress_MetricsEntry(): ComputeProgress_MetricsEntry {
  return { key: "", value: "" };
}

export const ComputeProgress_MetricsEntry: MessageFns<ComputeProgress_MetricsEntry> = {
  encode(message: ComputeProgress_MetricsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ComputeProgress_MetricsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseComputeProgress_MetricsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ComputeProgress_MetricsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: ComputeProgress_MetricsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ComputeProgress_MetricsEntry>, I>>(base?: I): ComputeProgress_MetricsEntry {
    return ComputeProgress_MetricsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ComputeProgress_MetricsEntry>, I>>(object: I): ComputeProgress_MetricsEntry {
    const message = createBaseComputeProgress_MetricsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseComputeResult(): ComputeResult {
  return { taskId: "", outputs: [], summary: {}, metadata: undefined };
}

export const ComputeResult: MessageFns<ComputeResult> = {
  encode(message: ComputeResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.taskId !== "") {
      writer.uint32(10).string(message.taskId);
    }
    for (const v of message.outputs) {
      DataRef.encode(v!, writer.uint32(18).fork()).join();
    }
    Object.entries(message.summary).forEach(([key, value]) => {
      ComputeResult_SummaryEntry.encode({ key: key as any, value }, writer.uint32(26).fork()).join();
    });
    if (message.metadata !== undefined) {
      Metadata.encode(message.metadata, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ComputeResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseComputeResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.taskId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.outputs.push(DataRef.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const entry3 = ComputeResult_SummaryEntry.decode(reader, reader.uint32());
          if (entry3.value !== undefined) {
            message.summary[entry3.key] = entry3.value;
          }
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.metadata = Metadata.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ComputeResult {
    return {
      taskId: isSet(object.taskId) ? globalThis.String(object.taskId) : "",
      outputs: globalThis.Array.isArray(object?.outputs) ? object.outputs.map((e: any) => DataRef.fromJSON(e)) : [],
      summary: isObject(object.summary)
        ? Object.entries(object.summary).reduce<{ [key: string]: string }>((acc, [key, value]) => {
          acc[key] = String(value);
          return acc;
        }, {})
        : {},
      metadata: isSet(object.metadata) ? Metadata.fromJSON(object.metadata) : undefined,
    };
  },

  toJSON(message: ComputeResult): unknown {
    const obj: any = {};
    if (message.taskId !== "") {
      obj.taskId = message.taskId;
    }
    if (message.outputs?.length) {
      obj.outputs = message.outputs.map((e) => DataRef.toJSON(e));
    }
    if (message.summary) {
      const entries = Object.entries(message.summary);
      if (entries.length > 0) {
        obj.summary = {};
        entries.forEach(([k, v]) => {
          obj.summary[k] = v;
        });
      }
    }
    if (message.metadata !== undefined) {
      obj.metadata = Metadata.toJSON(message.metadata);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ComputeResult>, I>>(base?: I): ComputeResult {
    return ComputeResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ComputeResult>, I>>(object: I): ComputeResult {
    const message = createBaseComputeResult();
    message.taskId = object.taskId ?? "";
    message.outputs = object.outputs?.map((e) => DataRef.fromPartial(e)) || [];
    message.summary = Object.entries(object.summary ?? {}).reduce<{ [key: string]: string }>((acc, [key, value]) => {
      if (value !== undefined) {
        acc[key] = globalThis.String(value);
      }
      return acc;
    }, {});
    message.metadata = (object.metadata !== undefined && object.metadata !== null)
      ? Metadata.fromPartial(object.metadata)
      : undefined;
    return message;
  },
};

function createBaseComputeResult_SummaryEntry(): ComputeResult_SummaryEntry {
  return { key: "", value: "" };
}

export const ComputeResult_SummaryEntry: MessageFns<ComputeResult_SummaryEntry> = {
  encode(message: ComputeResult_SummaryEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ComputeResult_SummaryEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseComputeResult_SummaryEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ComputeResult_SummaryEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: ComputeResult_SummaryEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ComputeResult_SummaryEntry>, I>>(base?: I): ComputeResult_SummaryEntry {
    return ComputeResult_SummaryEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ComputeResult_SummaryEntry>, I>>(object: I): ComputeResult_SummaryEntry {
    const message = createBaseComputeResult_SummaryEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseComputeFailure(): ComputeFailure {
  return { taskId: "", reason: "", details: {}, metadata: undefined };
}

export const ComputeFailure: MessageFns<ComputeFailure> = {
  encode(message: ComputeFailure, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.taskId !== "") {
      writer.uint32(10).string(message.taskId);
    }
    if (message.reason !== "") {
      writer.uint32(18).string(message.reason);
    }
    Object.entries(message.details).forEach(([key, value]) => {
      ComputeFailure_DetailsEntry.encode({ key: key as any, value }, writer.uint32(26).fork()).join();
    });
    if (message.metadata !== undefined) {
      Metadata.encode(message.metadata, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ComputeFailure {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseComputeFailure();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.taskId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const entry3 = ComputeFailure_DetailsEntry.decode(reader, reader.uint32());
          if (entry3.value !== undefined) {
            message.details[entry3.key] = entry3.value;
          }
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.metadata = Metadata.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ComputeFailure {
    return {
      taskId: isSet(object.taskId) ? globalThis.String(object.taskId) : "",
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
      details: isObject(object.details)
        ? Object.entries(object.details).reduce<{ [key: string]: string }>((acc, [key, value]) => {
          acc[key] = String(value);
          return acc;
        }, {})
        : {},
      metadata: isSet(object.metadata) ? Metadata.fromJSON(object.metadata) : undefined,
    };
  },

  toJSON(message: ComputeFailure): unknown {
    const obj: any = {};
    if (message.taskId !== "") {
      obj.taskId = message.taskId;
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    if (message.details) {
      const entries = Object.entries(message.details);
      if (entries.length > 0) {
        obj.details = {};
        entries.forEach(([k, v]) => {
          obj.details[k] = v;
        });
      }
    }
    if (message.metadata !== undefined) {
      obj.metadata = Metadata.toJSON(message.metadata);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ComputeFailure>, I>>(base?: I): ComputeFailure {
    return ComputeFailure.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ComputeFailure>, I>>(object: I): ComputeFailure {
    const message = createBaseComputeFailure();
    message.taskId = object.taskId ?? "";
    message.reason = object.reason ?? "";
    message.details = Object.entries(object.details ?? {}).reduce<{ [key: string]: string }>((acc, [key, value]) => {
      if (value !== undefined) {
        acc[key] = globalThis.String(value);
      }
      return acc;
    }, {});
    message.metadata = (object.metadata !== undefined && object.metadata !== null)
      ? Metadata.fromPartial(object.metadata)
      : undefined;
    return message;
  },
};

function createBaseComputeFailure_DetailsEntry(): ComputeFailure_DetailsEntry {
  return { key: "", value: "" };
}

export const ComputeFailure_DetailsEntry: MessageFns<ComputeFailure_DetailsEntry> = {
  encode(message: ComputeFailure_DetailsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ComputeFailure_DetailsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseComputeFailure_DetailsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ComputeFailure_DetailsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: ComputeFailure_DetailsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ComputeFailure_DetailsEntry>, I>>(base?: I): ComputeFailure_DetailsEntry {
    return ComputeFailure_DetailsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ComputeFailure_DetailsEntry>, I>>(object: I): ComputeFailure_DetailsEntry {
    const message = createBaseComputeFailure_DetailsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000).toString();
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
