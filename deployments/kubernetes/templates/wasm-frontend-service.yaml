apiVersion: v1
kind: Service
metadata:
  name: wasm-frontend
  labels:
    app: wasm-frontend
spec:
  type: { { .Values.wasmFrontend.service.type } }
  ports:
    - port: 8081
      targetPort: 8081
  selector:
    app: wasm-frontend
